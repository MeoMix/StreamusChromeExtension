define(function() {
    'use strict';

    var SimpleMenuItem = Backbone.Model.extend({
        defaults: function() {
            return {
                
            };
        }
    });

    return SimpleMenuItem;
});