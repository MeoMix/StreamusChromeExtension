define(function () {
    'use strict';
    //  TODO: Seems odd to have a model with nothing in it. I think I destroy the model and listen to an event, but eh.. really?
    var PlaylistsArea = Backbone.Model.extend({
    });

    return PlaylistsArea;
});