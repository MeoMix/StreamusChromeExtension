define(function () {
    'use strict';

    //  TODO: This won't be necessary in a while but I'm keeping it while I transition, once listening to destroy is gone it'll be removed.
    var PlaylistsArea = Backbone.Model.extend({

    });

    return PlaylistsArea;
});